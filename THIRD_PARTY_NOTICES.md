# Third-party notices

## u8g2

- Fetched by `make deps` (or `make getlibs`) into `libs/u8g2/` via `deps/u8g2.mtb` (gitignored)
- Upstream: https://github.com/olikraus/u8g2 (tag 2.36.9; build uses `csrc/` only)
- License: BSD (see upstream `LICENSE` in the u8g2 repository)

## display-oled-ssd1306 (ModusToolbox library)

- Fetched by `make getlibs` into `mtb_shared/display-oled-ssd1306/`
- Upstream: https://github.com/Infineon/display-oled-ssd1306
- License: Apache-2.0

## Infineon ModusToolbox components

BSP, PDL, HAL, CapSense, and other libraries under `mtb_shared/` are fetched by
`make getlibs` and remain under their respective licenses.
